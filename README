Data Acquisition and Control Application (dactl)

Version 0.1

================================================================================

Notes:

Possible consider creating RPM using the command

fpm -s dir -t rpm -C dactl --name dactl --version 0.2.0 --iteration 1 \
--depends libgee --depends gtk3 --depends clutter \
--depends gsl --depends libxml2

  (might need a folder name following command, everything is placed there)
  (see: http://www.openlogic.com/wazi/bid/296143/quickly-create-custom-software-packages-with-fpm)

================================================================================

Dependencies:

libcld - dc5c110
  - brabender code implemented by above git ref
